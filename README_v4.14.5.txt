BOZO v4.14.5

Upload index.html and app-v4.14.5.js to the site root.
No SQL needs to be run.

Expected result after deploy:
1. Master Library should show 100 for the current 100-game import (assuming no search/result/year filter is active).
2. Selecting a game while the page is scrolled should bring the Master Game viewer top back below the sticky navbar instead of clipping the game title/result.
