Upload these two site files:
- index.html
- app-v4.14.8.js

Then hard-refresh the site.

Tests:
1. Open Train > Master Game Puzzles.
2. Solve/finish one master puzzle, then load another. The second board should
   accept piece clicks immediately.
3. Right-click highlight a square and draw several arrows (including a knight
   arrow). Their centers should line up with the rendered square centers.
